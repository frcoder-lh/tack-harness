# Handoff — 会话交接

将当前会话压缩成交接文档，以便另一个 agent 继续工作。

## 过程

1. 编写总结当前会话的交接文档
2. 保存到用户操作系统的临时目录
3. 包含"建议技能"部分，命名下一个 agent 应该使用的 skill
4. 不要复制其他工件中已捕获的内容（规范、计划、技术设计、提交、差异）。通过路径或 URL 引用它们
5. 清理敏感信息，如 API 密钥、密码或个人身份信息

如果用户传递了参数，将其作为下一个会话的焦点描述，并据此调整文档。